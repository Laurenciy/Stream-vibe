import MoviesBanner from "@/layouts/Section/MoviesBanner";

export const metadata = {
  title: 'Movies & Shows',
}

export default function () {
  return (
    <div>
      <MoviesBanner/>
    </div>
  )
}
